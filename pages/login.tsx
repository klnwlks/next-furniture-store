import styles from '../styles/Login.module.scss'
import type { FC } from 'react'

const Login: FC = () => {
  return ( 
    <div id={styles.login}>
      <p>login</p>
      <form>
      </form>
    </div>
  )
}

export default Login
